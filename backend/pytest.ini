[pytest]
markers =
    ollama_integration: Live Ollama reasoning-agent integration tests (slow, require running Ollama server).

# Exclude Ollama integration tests from the default suite.
# Run them explicitly with:  python -m pytest -m ollama_integration -v
addopts = -m "not ollama_integration"
